version: '3'
services:
  app:
    build: .
    ports:
      - "5000:3000" # You open http://<IP>:5000 (Avoids Jenkins 8080)
    depends_on:
      - db

  db:
    image: mariadb
    environment:
      MARIADB_ROOT_PASSWORD: 123
